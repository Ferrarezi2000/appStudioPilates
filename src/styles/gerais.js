import cores from './cores';
import metricas from './metricas';

export default {
    box: {
        backgroundColor: cores.white,
        borderRadius: metricas.baseRadius,
        padding: metricas.basePadding,
        margin: metricas.baseMargin
    }
};
